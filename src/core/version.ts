// Single source of truth for the displayed build version.
// Shown in the menu and the debug overlay so screenshots and playtest notes are
// easy to attribute to a specific build (see docs/08-dev-workflow.md).
export const GAME_VERSION = '0.2.0';
